namespace GemTD.UI
{
    /// <summary>Placeholder so the UI assembly compiles until HUD is built.</summary>
    public static class UiAssemblyMarker
    {
        public const string Name = "GemTD.UI";
    }
}
